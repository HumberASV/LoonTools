# configMTU

Script is used to configure the MTU (Maximum Transmission Unit) settings for the network interfaces used by the LoonDock-managed containers.
It's related to zedx wrapper and is necessary to ensure proper communication between the host machine and the containers, especially when using the ZED camera with high data rates.